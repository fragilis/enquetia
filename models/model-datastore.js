'use strict';

const questions = require('./questions');
const answers = require('./answers');
const votes = require('./votes');

module.exports = {
  questions: questions,
  answers: answers,
  votes: votes
}

